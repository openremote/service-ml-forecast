# Training Service
#
# This service is responsible for training the model. The training data is read
# from the OpenRemote API based on the model config. The training is done on a
# schedule based on the model config.
#
# The trained model is then stored in the local file system.
#


class TrainingService:
    def __init__(self) -> None:
        pass

    def train(self) -> None:
        pass
